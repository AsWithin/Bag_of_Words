# 本项目依赖（Python 3.12）
# 安装命令：pip install -r requirements.txt
#
# 说明：gensim 会自动拉取 scipy、smart_open、wrapt 等传递依赖；
#       scikit-learn 会自动拉取 joblib、threadpoolctl；
#       这里只列出直接 import 到的包。

beautifulsoup4==4.15.0
gensim==4.4.0
nltk==3.10.3
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.0
